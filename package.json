{
  "name": "prefabricadas-premium-netlify",
  "version": "2.0.0",
  "description": "Sistema de cotización para casas prefabricadas",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Building...'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "node-fetch": "^2.6.12",
    "@sendgrid/mail": "^7.7.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}